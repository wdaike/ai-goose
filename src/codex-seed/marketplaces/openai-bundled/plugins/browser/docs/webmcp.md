# WebMCP
After browser interactions, `Browser notifications` may report page-defined
WebMCP tools and their current schemas. When a WebMCP tool covers the requested
functionality, prefer it over all other page-interaction modalities. Call
`listTools()` only to refresh.
